[Unit]
Description=Back up the four-path portfolio SQLite database

[Service]
Type=oneshot
WorkingDirectory=/opt/four-path-portfolio
EnvironmentFile=/opt/four-path-portfolio/.env
ExecStart=/usr/bin/docker compose exec -T portfolio python -m server.ops backup
